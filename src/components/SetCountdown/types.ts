export type propsTypes = {
    dataTime: {
      minutes: {
        dec: number;
        unit: number,
      },
      seconds: {
        dec: number,
        unit: number,
      },
  };
}

export type timeTypes = {
    dec: number;
    unit: number;
}
